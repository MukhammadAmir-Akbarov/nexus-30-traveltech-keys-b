import { ArrivalCard } from '@/components/ArrivalCard';

/**
 * Экран «я на месте».
 *
 * Отдельным маршрутом, а не блоком на главной: на главной он попадался бы
 * человеку, который сидит дома и планирует поездку, — там этот вопрос
 * бессмысленный. Сюда приходят с уведомления и по нижней вкладке, то есть
 * уже стоя у объекта.
 *
 * Страница намеренно пустая вокруг карточки: у объекта человек читает
 * с одной руки, на солнце, и лишние блоки здесь только мешают.
 */
export const metadata = {
  title: 'Рядом — Надёжный спутник',
};

export default function NearbyPage() {
  return (
    <div className="flex flex-col gap-4">
      <ArrivalCard />
    </div>
  );
}
